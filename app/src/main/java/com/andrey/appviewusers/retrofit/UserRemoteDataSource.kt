package com.andrey.appviewusers.retrofit

class UserRemoteDataSource(
    private val retrofitService: RetrofitService
    ){

    //suspend fun getUsers() = retrofitService.getSomeData().results.map { it }
}
